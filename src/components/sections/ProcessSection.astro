---
import { Icon } from 'astro-icon/components';
// src/components/sections/ProcessSection.astro
const processSteps = [
  {
    title: "Descubrimiento y Estrategia",
    description: "Comprendemos a fondo tus objetivos, audiencia y desafíos para definir una hoja de ruta y estrategia claras.",
    icon: "mdi:clipboard-text-search-outline"
  },
  {
    title: "Diseño UI/UX",
    description: "Creamos prototipos y diseños de interfaz intuitivos y atractivos, centrados en la experiencia del usuario final.",
    icon: "mdi:palette-outline"
  },
  {
    title: "Desarrollo e Iteración",
    description: "Construimos la solución utilizando metodologías ágiles, con ciclos de feedback regulares para asegurar la alineación.",
    icon: "mdi:code-braces"
  },
  {
    title: "Pruebas y Entrega",
    description: "Realizamos pruebas exhaustivas para garantizar la calidad y funcionalidad antes del despliegue final.",
    icon: "mdi:rocket-launch-outline"
  },
  {
    title: "Mantenimiento y Crecimiento",
    description: "Ofrecemos soporte post-lanzamiento, monitorizamos el rendimiento y proponemos mejoras continuas.",
    icon: "mdi:lifebuoy"
  }
];
---

<section class="py-20 bg-gray-50 dark:bg-gray-800/50" id="process">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"> {/* Increased max-width */}
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-4">
        Nuestro Proceso {/* Changed Title */}
      </h2>
      <p class="text-lg text-gray-600 dark:text-gray-300 max-w-3xl mx-auto"> {/* Increased max-width */}
        Un enfoque transparente y estructurado para garantizar el éxito del proyecto y entregar el máximo valor. {/* Changed text */}
      </p>
    </div>

    {/* Adjusted grid columns for better responsiveness */}
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8">
      {processSteps.map((step, index) => (
        <div class="process-step-container group relative overflow-hidden p-10 rounded-2xl shadow-lg border border-gray-100 dark:border-gray-700/50 cursor-pointer transition-all duration-300 ease-in-out h-96 flex flex-col justify-center items-center text-center"> {/* Increased h-64, p-8, rounded-2xl, shadow-lg */}
          {/* Step Number */}
          <span class="absolute top-4 right-4 text-5xl font-bold text-gray-200 dark:text-gray-600/50 group-hover:text-gray-300 dark:group-hover:text-gray-500/70 transition-colors duration-300 z-0">
            {String(index + 1).padStart(2, '0')}
          </span>

          {/* Visible Content (Icon + Title) */}
          <div class="visible-content transition-opacity duration-300 ease-in-out group-hover:opacity-0 relative z-10"> {/* Added z-10 */}
            <div class="mb-5 text-blue-500 dark:text-purple-400"> {/* Adjusted color */}
              <Icon name={step.icon} class="w-14 h-14 mx-auto" /> {/* Slightly larger icon */}
            </div>
            <h3 class="text-xl font-semibold text-gray-800 dark:text-white">{step.title}</h3> {/* Increased font size, adjusted color */}
          </div>

          {/* Hidden Content (Description) - Revealed on Hover */}
          <div class="hidden-content absolute inset-0 bg-gradient-to-br from-blue-500 to-blue-700 dark:from-purple-600 dark:to-purple-800 p-8 flex flex-col justify-center items-center text-white opacity-0 group-hover:opacity-100 transition-all duration-300 ease-in-out transform scale-95 group-hover:scale-100"> {/* Adjusted gradient, padding, transform */}
            <h4 class="text-xl font-semibold mb-3">{step.title}</h4> {/* Increased font size, mb */}
            <p class="text-base">{step.description}</p> {/* Increased font size */}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .process-step-container {
    /* Subtle gradient for modern look */
    background-color: white; /* Fallback */
    background-image: linear-gradient(to bottom right, white, #f3f4f6); /* Very light gradient */
  }
  .dark .process-step-container {
     background-color: #1f2937; /* Dark fallback */
     background-image: linear-gradient(to bottom right, #374151, #1f2937); /* Darker subtle gradient */
  }

  /* Ensure content is centered when description is hidden */
   .process-step-container .visible-content {
     width: 100%;
   }

  /* Refined transition for hidden content */
  .process-step-container .hidden-content {
    transition: opacity 0.3s ease-in-out, transform 0.3s ease-in-out;
  }
</style>
