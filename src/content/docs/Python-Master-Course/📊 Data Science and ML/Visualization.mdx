---
title: "Visualización de Datos en Python: Matplotlib, Seaborn y Plotly"
description: "Un tutorial sobre visualización de datos en Python: Matplotlib, Seaborn y Plotly para crear gráficos estáticos e interactivos."
---

import { Card, CardGrid  } from '@astrojs/starlight/components';
import { Aside } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Code } from '@astrojs/starlight/components';

<CardGrid>
  <Card title="¡Visualiza tus Datos!" icon="chart-bar">
    Aprende a crear gráficos con Matplotlib, Seaborn y Plotly para entender mejor tus datos.
  </Card>
  <Card title="Gráficos Estáticos e Interactivos" icon="activity">
   Explora cómo usar estas librerías para crear visualizaciones efectivas y personalizadas.
  </Card>
</CardGrid>

<Aside type="note" title="Importante 📝">
    La visualización de datos es fundamental para comprender, comunicar y analizar datos de manera efectiva.
</Aside>

# Visualización de Datos en Python: Transformando Datos en Gráficos

En este post, exploraremos en detalle cómo visualizar datos en Python utilizando tres librerías populares: Matplotlib, Seaborn y Plotly. Aprenderás a crear gráficos estáticos con Matplotlib y Seaborn, y a crear gráficos interactivos con Plotly. Cubriremos:

1.  **Matplotlib:** Cómo crear gráficos básicos y personalizados.
2.  **Seaborn:**  Cómo crear gráficos estadísticos de forma sencilla.
3.  **Plotly:** Cómo crear gráficos interactivos y con funcionalidades avanzadas.

## 1. Matplotlib <Badge text="El Clásico de la Visualización" variant="tip" />

Matplotlib es una librería fundamental para la creación de gráficos estáticos en Python. Permite crear gráficos de diferentes tipos y personalizar todos sus aspectos.

### Instalación

```bash
pip install matplotlib
```

### Gráficos Básicos
* `plt.plot(x, y)`: Crea un gráfico de líneas.
* `plt.scatter(x, y)`: Crea un gráfico de dispersión.
* `plt.bar(x, height)`: Crea un gráfico de barras.
* `plt.hist(x)`: Crea un histograma.
* `plt.pie(x)`: Crea un gráfico de torta.
* `plt.show()`: Muestra el gráfico.
*  `plt.title(titulo)`: Define el título del gráfico.
*  `plt.xlabel(etiqueta_x)`: Define la etiqueta del eje X.
*  `plt.ylabel(etiqueta_y)`: Define la etiqueta del eje Y.
* `plt.legend()`: Muestra la leyenda de los elementos graficados.
```python
import matplotlib.pyplot as plt
import numpy as np
# Grafico de Lineas
x = np.linspace(0, 10, 100)
y = np.sin(x)
plt.plot(x, y, label="sin(x)")
plt.title("Gráfico de Líneas")
plt.xlabel("Eje X")
plt.ylabel("Eje Y")
plt.legend()
plt.show()

# Grafico de Dispersion
x = np.random.rand(100)
y = np.random.rand(100)
plt.scatter(x, y, label="puntos")
plt.title("Gráfico de Dispersión")
plt.xlabel("Eje X")
plt.ylabel("Eje Y")
plt.legend()
plt.show()
# Grafico de Barras
categorias = ["A", "B", "C", "D"]
valores = [25, 50, 75, 100]
plt.bar(categorias, valores, label="categorias")
plt.title("Gráfico de Barras")
plt.xlabel("Categorias")
plt.ylabel("Valores")
plt.legend()
plt.show()

#Histograma
datos = np.random.randn(1000)
plt.hist(datos, bins=20, label="datos")
plt.title("Histograma")
plt.xlabel("Valores")
plt.ylabel("Frecuencia")
plt.legend()
plt.show()
# Grafico de Torta
etiquetas = ["A", "B", "C", "D"]
valores = [25, 25, 25, 25]
plt.pie(valores, labels=etiquetas, autopct='%1.1f%%')
plt.title("Gráfico de Torta")
plt.show()

```

### Personalización de Gráficos
*   Cambiar el estilo de línea: `plt.plot(x, y, linestyle="--")`.
*   Cambiar el color de línea: `plt.plot(x, y, color="red")`.
*   Cambiar el marcador de los puntos: `plt.scatter(x, y, marker="o")`.
* Cambiar el tamaño de los marcadores y el tamaño de la línea: `plt.plot(x,y, linewidth=2)`, `plt.scatter(x, y, s=10)`.
*  Cambiar el rango de los ejes: `plt.xlim(0,10)`, `plt.ylim(0,10)`.
*  Añadir texto en un punto específico :`plt.text(x,y, "texto")`.
*  Añadir una grilla: `plt.grid(True)`.
*  Cambiar el tamaño del grafico: `plt.figure(figsize=(8,6))`.

```python
import matplotlib.pyplot as plt
import numpy as np
x = np.linspace(0, 10, 100)
y = np.sin(x)
plt.figure(figsize=(8,6))
plt.plot(x, y, linestyle="--", color="red", linewidth=2, label="sin(x)")
plt.scatter(x[::10],y[::10], marker="o", s=10, label = "puntos")
plt.title("Gráfico de Líneas Personalizado")
plt.xlabel("Eje X")
plt.ylabel("Eje Y")
plt.xlim(0,10)
plt.ylim(-1.5,1.5)
plt.text(3,0.5,"Un texto", fontsize=12)
plt.grid(True)
plt.legend()
plt.show()

```
### Múltiples Gráficos

* `plt.subplot(filas, columnas, indice)`: Crea un subplot dentro de una figura.

```python
import matplotlib.pyplot as plt
import numpy as np
x = np.linspace(0, 10, 100)
y1 = np.sin(x)
y2 = np.cos(x)
plt.figure(figsize=(10, 5))
plt.subplot(1, 2, 1)
plt.plot(x, y1)
plt.title("Seno")
plt.subplot(1, 2, 2)
plt.plot(x, y2)
plt.title("Coseno")
plt.show()
```
<Aside type="note" title="Nota 📝">
  Matplotlib es una librería muy completa pero puede ser un poco verbosa para graficos complejos.
</Aside>

## 2. Seaborn <Badge text="Gráficos Estadísticos" variant="note" />

Seaborn es una librería basada en Matplotlib que proporciona una interfaz de alto nivel para crear gráficos estadísticos de forma sencilla y atractiva.

### Instalación

```bash
pip install seaborn
```

### Gráficos Estadísticos

* `sns.histplot(data, x, hue)`:  Crea histogramas.
*  `sns.countplot(data, x, hue)`: Crea conteo de valores.
*  `sns.scatterplot(data, x, y, hue)`: Crea gráficos de dispersion.
*  `sns.lineplot(data, x, y, hue)`: Crea graficos de líneas.
*  `sns.boxplot(data, x, y)`: Crea diagramas de cajas.
*  `sns.violinplot(data, x, y)`: Crea diagramas de violin.
* `sns.heatmap(data)`: Crea un mapa de calor.
*  `sns.pairplot(data)`: Crea un gráfico de pares de variables.

```python
import seaborn as sns
import matplotlib.pyplot as plt
import pandas as pd

data = {
    "nombre":["Juan", "Maria", "Pedro","Ana", "Luis","Sofia"],
    "edad": [30, 25, 40, 18, 22,30],
    "ciudad": ["Madrid", "Barcelona", "Valencia","Madrid", "Valencia", "Barcelona"],
    "estudios": ["bachiller","universitario", "doctorado","bachiller","universitario","doctorado"],
    "puntaje":[8,6,9,5,7,10]
}
df = pd.DataFrame(data)
sns.histplot(df, x="edad", hue="ciudad")
plt.title("Histograma de Edades")
plt.show()

sns.countplot(df, x="ciudad", hue="estudios")
plt.title("Conteo de Ciudades")
plt.show()

sns.scatterplot(df, x="edad", y="puntaje", hue="ciudad")
plt.title("Dispersión de Edades vs Puntaje")
plt.show()

sns.lineplot(df, x="edad", y="puntaje", hue="ciudad")
plt.title("Lineas de Edades vs Puntaje")
plt.show()

sns.boxplot(df, x="ciudad", y="edad")
plt.title("Boxplot de Edades por ciudad")
plt.show()

sns.violinplot(df, x="ciudad", y="edad")
plt.title("Violinplot de Edades por ciudad")
plt.show()

corr = df[["edad", "puntaje"]].corr()
sns.heatmap(corr, annot=True)
plt.title("Mapa de Calor de Correlación")
plt.show()

sns.pairplot(df[["edad", "puntaje"]])
plt.title("PairPlot")
plt.show()

```
<Aside type="caution" title="Precaución ⚠️">
  Seaborn es una librería muy práctica para graficar estadisticas, es muy fácil crear graficos bonitos y complejos con poco código.
</Aside>

## 3. Plotly <Badge text="Gráficos Interactivos" variant="success" size="large" />

Plotly es una librería para crear gráficos interactivos y con funcionalidades avanzadas, muy útil para dashboards o visualizaciones web.

### Instalación

```bash
pip install plotly
```
### Gráficos Interactivos

*  `px.line(data, x, y)`: Crea un gráfico de líneas interactivo.
*  `px.scatter(data, x, y)`: Crea un gráfico de dispersión interactivo.
*  `px.bar(data, x, y)`: Crea un gráfico de barras interactivo.
* `px.pie(data, values, names)`: Crea un grafico de torta interactivo
*   `fig.show()`: Muestra el gráfico.

```python
import plotly.express as px
import pandas as pd

data = {
    "nombre":["Juan", "Maria", "Pedro","Ana", "Luis","Sofia"],
    "edad": [30, 25, 40, 18, 22,30],
    "ciudad": ["Madrid", "Barcelona", "Valencia","Madrid", "Valencia", "Barcelona"],
    "estudios": ["bachiller","universitario", "doctorado","bachiller","universitario","doctorado"],
    "puntaje":[8,6,9,5,7,10]
}
df = pd.DataFrame(data)

fig = px.line(df, x="edad", y="puntaje", color="ciudad", title="Lineas de Edades vs Puntaje")
fig.show()

fig = px.scatter(df, x="edad", y="puntaje", color="ciudad", title="Dispersión de Edades vs Puntaje")
fig.show()

fig = px.bar(df, x="ciudad", y="edad", color="estudios", title="Barras de Edades por ciudad y estudios")
fig.show()

fig = px.pie(df, values="edad", names="ciudad", title="Torta de Edades por ciudad")
fig.show()
```
### Personalización de gráficos

* Se puede personalizar los colores, el diseño y muchos otros aspectos de los gráficos interactivos.
* Plotly es ideal para gráficos que se deben mostrar en la web.

<Aside type="note" title="Nota 📝">
  Plotly es una librería muy potente para crear gráficos interactivos, pero puede ser un poco más compleja de utilizar.
</Aside>

## Conclusión

La visualización de datos es una habilidad esencial para cualquier analista o científico de datos. En este post, exploramos Matplotlib, Seaborn y Plotly, aprendiendo a crear gráficos estáticos e interactivos. ¡Ahora tienes las herramientas para convertir tus datos en visualizaciones efectivas!

<Badge text="¡Sigue Visualizando!" variant="success" size="large" />

En el próximo post, exploraremos Machine Learning básico.

import { LinkButton } from '@astrojs/starlight/components';

<LinkButton
  href="/docs/Python-Master-Course/intro"
  variant="primary"
  icon="arrow-left"
  iconPlacement="start"
>
  Volver al Inicio del Curso
</LinkButton>