---
title: "Control de Flujo en Python: Condicionales, Bucles y Match Cases"
description: "Aprende a controlar el flujo de tus programas en Python usando condicionales, bucles y match cases, para tomar decisiones y ejecutar acciones repetitivas."
---

import { Card, CardGrid  } from '@astrojs/starlight/components';
import { Aside } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Code } from '@astrojs/starlight/components';

<CardGrid>
  <Card title="¡Toma el Control!" icon="shuffle">
    Aprende a dirigir el flujo de tus programas con decisiones y repeticiones.
  </Card>
  <Card title="Flexibilidad y Poder" icon="lightning">
    Explora cómo los bucles, condicionales y match cases hacen tus programas más versátiles.
  </Card>
</CardGrid>

<Aside type="note" title="Importante 📝">
  El control de flujo es esencial para que tus programas tomen decisiones y realicen tareas repetitivas de manera eficiente.
</Aside>

# Control de Flujo en Python: Decidiendo y Repitiendo Acciones

En este post, exploraremos en detalle el control de flujo en Python. El control de flujo te permite tomar decisiones en tu código y ejecutar acciones de manera repetitiva. Cubriremos:

1.  **Condicionales y Operadores:** Toma decisiones basadas en condiciones.
2.  **Bucles `for` y `while`:** Ejecuta bloques de código repetidamente.
3.  **`break` y `continue`:**  Controla la ejecución dentro de los bucles.
4.  **`match case` (Python 3.10+):**  Una forma más elegante de manejar múltiples condiciones.

## 1. Condicionales y Operadores <Badge text="Toma de Decisiones" variant="tip" />

Las estructuras condicionales (`if`, `elif`, `else`) te permiten ejecutar diferentes bloques de código según si una condición es verdadera o falsa.

### Estructura `if`

```python
edad = 20
if edad >= 18:
    print("Eres mayor de edad")
```

### Estructura `if-else`

```python
edad = 15
if edad >= 18:
    print("Eres mayor de edad")
else:
    print("Eres menor de edad")
```

### Estructura `if-elif-else`

```python
nota = 7
if nota >= 9:
    print("Excelente")
elif nota >= 7:
    print("Aprobado")
elif nota >= 5:
    print("Suficiente")
else:
    print("Reprobado")
```

### Operadores de Comparación

*   `==` (Igual a)
*   `!=` (Diferente a)
*   `>` (Mayor que)
*   `<` (Menor que)
*   `>=` (Mayor o igual a)
*   `<=` (Menor o igual a)

### Operadores Lógicos

*   `and` (Y lógico): Retorna `True` solo si ambos operandos son `True`.
*   `or` (O lógico): Retorna `True` si al menos uno de los operandos es `True`.
*   `not` (NO lógico): Niega el valor del operando.

```python
edad = 25
tiene_licencia = True

if edad >= 18 and tiene_licencia:
    print("Puede conducir")

if edad < 18 or not tiene_licencia:
    print("No puede conducir")
```

## 2. Bucles `for` y `while` <Badge text="Repeticiones" variant="note" />

Los bucles te permiten ejecutar un bloque de código repetidamente. Python ofrece dos tipos principales de bucles: `for` y `while`.

### Bucle `for`
El bucle `for` se utiliza para iterar sobre una secuencia (lista, tupla, string, diccionario, etc.) o un rango de números.

*   **Iterando sobre una lista:**

    ```python
    colores = ["rojo", "verde", "azul"]
    for color in colores:
        print(color)
    ```
*   **Iterando sobre un rango de números:**

    ```python
    for i in range(5):
        print(i)  # 0, 1, 2, 3, 4
    ```

*   **Iterando sobre un string:**
    ```python
    texto = "Python"
    for char in texto:
        print(char) # P,y,t,h,o,n
    ```

*   **Iterando sobre un diccionario:**
    ```python
    persona = {
        "nombre": "Juan",
        "edad": 30,
        "ciudad": "Madrid"
    }
    for key, value in persona.items():
        print(f"{key}: {value}")
    ```

### Bucle `while`

El bucle `while` ejecuta un bloque de código mientras una condición sea verdadera.

```python
contador = 0
while contador < 5:
    print(contador)
    contador += 1
```
<Aside type="caution" title="Precaución ⚠️">
   Asegúrate de que la condición de un bucle `while` eventualmente se vuelva falsa para evitar bucles infinitos.
</Aside>

## 3. `break` y `continue` <Badge text="Control de Bucles" variant="success" size="large" />

`break` y `continue` son palabras clave que te permiten controlar la ejecución dentro de los bucles.

*   **`break`:** Termina el bucle inmediatamente.

    ```python
    for i in range(10):
        if i == 5:
            break
        print(i)  # 0, 1, 2, 3, 4
    ```

*   **`continue`:** Salta a la siguiente iteración del bucle, ignorando el resto del código del bloque actual.

    ```python
    for i in range(10):
        if i % 2 == 0:
            continue
        print(i)  # 1, 3, 5, 7, 9
    ```

## 4. `match case` (Python 3.10+) <Badge text="Condicionales Avanzadas" variant="tip" />

El `match case` es una forma más elegante y legible de manejar múltiples condiciones.  Es similar al `switch` en otros lenguajes.

```python
def obtener_estado(codigo):
    match codigo:
        case 200:
            return "OK"
        case 404:
            return "No encontrado"
        case 500:
            return "Error del servidor"
        case _: # _ es el caso por defecto
            return "Código desconocido"

print(obtener_estado(200)) # OK
print(obtener_estado(404)) # No encontrado
print(obtener_estado(501)) # Código desconocido
```

### Patrones con `match case`

El `match case` soporta patrones más complejos:
*   **Patrones de Secuencia:**

    ```python
    def analizar_punto(punto):
        match punto:
            case (0, 0):
                print("Origen")
            case (x, 0):
                print(f"Eje X, x={x}")
            case (0, y):
                print(f"Eje Y, y={y}")
            case (x, y):
                 print(f"Punto ({x},{y})")
    analizar_punto((0,0)) # Origen
    analizar_punto((5,0)) # Eje X, x=5
    analizar_punto((0,2)) # Eje Y, y=2
    analizar_punto((1,2)) # Punto (1,2)
    ```

*   **Patrones con `if`:**

    ```python
    def analizar_valor(valor):
        match valor:
            case numero if numero > 0:
                print(f"Numero positivo {numero}")
            case numero if numero < 0:
                print(f"Numero negativo {numero}")
            case _:
               print("Es cero")
    analizar_valor(3) # Numero positivo 3
    analizar_valor(-2) # Numero negativo -2
    analizar_valor(0) # Es cero
    ```

<Aside type="note" title="Nota 📝">
    El `match case` es una herramienta muy útil para hacer tu código más legible y expresivo, especialmente cuando tienes muchas condiciones a evaluar.
</Aside>

## Conclusión

El control de flujo es fundamental para escribir programas útiles. En este post, exploramos condicionales, bucles, `break`, `continue` y el `match case`.  ¡Ahora puedes hacer que tus programas tomen decisiones y realicen tareas repetitivas de manera eficiente!

<Badge text="¡Sigue Experimentando!" variant="success" size="large" />

En el próximo post, exploraremos funciones en Python.

import { LinkButton } from '@astrojs/starlight/components';

<LinkButton
  href="/docs/Python-Master-Course/intro"
  variant="primary"
  icon="arrow-left"
  iconPlacement="start"
>
  Volver al Inicio del Curso
</LinkButton>